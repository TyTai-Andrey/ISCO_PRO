import React from 'react';
import { Table } from '../../components/Table';

import './Main.scss';

export const Main: React.FC = () => {
  return (
    <div className="Main">
      <Table />
    </div>
  );
};
